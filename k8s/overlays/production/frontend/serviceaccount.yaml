# ============================================================
# SERVICE ACCOUNT — frontend
# A ServiceAccount is like an identity card for a pod.
# It controls what AWS resources the pod can access.
# We give each service its own identity (least privilege).
# ============================================================
apiVersion: v1
kind: ServiceAccount
metadata:
  name: frontend-sa
  namespace: lab
  labels:
    app: frontend
